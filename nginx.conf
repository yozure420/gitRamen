events {
    worker_connections 1024;
}

http {
    # 1. MIMEタイプの設定（CSSやMP3を正しく認識させるために必須）
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # 2. ログ形式の定義（デバッグしやすくするため）
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
    access_log  /var/log/nginx/access.log  main;

    server {
        listen 80;
        server_name app.gitramen.com;

        # 3. 502対策：バッファとタイムアウトの拡張
        proxy_buffers 8 32k;
        proxy_buffer_size 64k;
        proxy_busy_buffers_size 128k;
        
        client_max_body_size 10M; # 大きめのファイルアップロード等に備えて

        location / {
            proxy_pass http://gitramen_frontend:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/ {
            proxy_pass http://gitramen_backend:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
}